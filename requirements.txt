# Every package below is exact-pinned (2026-07-09) at its currently-tested resolved
# version. Previously most floated on `>=`, with no lockfile to catch a bad upstream
# release before it reached prod (found in the 2026-07-09 weakness audit — yt-dlp and
# google-genai in particular ship frequent breaking releases). Bump deliberately and
# re-run the test suite, don't let these drift back to loose bounds.
anthropic==0.109.1
python-dotenv==1.0.1
flask==3.1.3
Pillow==11.3.0  # capped <12.0 -- moviepy 2.2.1 requires pillow<12.0,>=9.2.0
reportlab==4.5.1
openai==2.41.1
# fastapi/uvicorn pinned to the versions the working image has run on — the rest of
# the manifest floats, but these two are load-bearing for the ASGI + WebSocket chat
# path, so they don't move without an explicit, tested bump.
fastapi==0.111.0
uvicorn[standard]==0.29.0
apscheduler==3.11.3
vtracer==0.6.15
python-multipart==0.0.32
requests==2.33.1
beautifulsoup4==4.15.0
lxml==6.1.1
PyNaCl==1.6.2
playwright==1.60.0
PyPDF2==3.0.1
# PyMuPDF (fitz) -- powers tools/planner_hyperlinker.py's real hyperlinked
# navigation/TOC/fillable AcroForm fields on the digital planner PDFs. Restored
# 2026-07-16 from the trash vault along with the rest of the planner-generation
# pipeline (was previously an undeclared dependency of a since-archived script).
PyMuPDF==1.28.0
moviepy==2.2.1
imageio-ffmpeg==0.6.0
# Google GenAI SDK — Veo 3.1 video (AI_VIDEO_ENGINE=veo) + Nano Banana / Gemini image
# (IMAGE_ENGINE=gemini) + Gemini native video understanding. Lazily imported, so its
# presence never affects startup; ships in the image so those features work once
# GEMINI_API_KEY is set. (Previously only in tools/api_server/requirements.txt.)
google-genai==2.10.0
# yt-dlp — fetches videos from URLs (YouTube, TikTok, direct .mp4, ~1000 sites) for the
# watch_video tool (tools/video_understanding.py). Lazily imported; pure-python wheel.
# Ships frequent breaking releases — bump this one deliberately, not opportunistically.
yt-dlp==2026.7.4
# openpyxl — builds the live Business Tracker workbook (tools/business_tracker.py,
# GET /api/business-tracker.xlsx). Previously only used by an unwired one-off script
# kept out of this file deliberately; now a real dependency since it's wired to a
# live endpoint.
openpyxl==3.1.5
